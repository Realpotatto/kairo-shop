[build]
builder = "nixpacks"

[deploy]
startCommand = "node --enable-source-maps api-server/dist/index.cjs"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
